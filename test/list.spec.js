import React from 'react';
import ActiveView from '../src/components/list';
import {
  mount,
  shallow,
} from 'enzyme';

describe('<List />',  () => {
	const props = {
		
	}
	const wrapper = mount

	it('', () => {
      
  });

  it('', () => {
      
  });

  it('', () => {
      
  });

  it('', () => {
      
  });
});